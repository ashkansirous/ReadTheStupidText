<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="ReadTheStupidText_App.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ReadTheStupidText_App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>

            <!-- Shared brand resources for the gradient headers (the control panel
                 defines equivalents inline; these back the activity-log window so
                 both windows read as the same product). -->
            <LinearGradientBrush x:Key="BrandGradient" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#5B57E8" Offset="0" />
                <GradientStop Color="#3B82F6" Offset="1" />
            </LinearGradientBrush>
            <SolidColorBrush x:Key="HeaderText" Color="#EBFFFFFF" />
            <SolidColorBrush x:Key="HeaderEyebrow" Color="#B3FFFFFF" />
            <SolidColorBrush x:Key="HeaderControl" Color="#33FFFFFF" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
